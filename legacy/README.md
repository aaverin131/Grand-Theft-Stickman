# Legacy

Snapshots of older code, kept for reference. Not imported by the running game.

- `main1.py` / `gameclass1.py` — Jan 2024 drafts of `main.py` / `gameclass.py`. Core logic is the same; these carry extra commented-out experiments (a tile-matrix world prototype, scaling/zoom code, draft `Assets`/`GameMap`/`Events_info` classes).
- `new.csv` — early save-format scratch file.

Originals `main.py` and `gameclass.py` will be moved here once the refactor's new entry point is in place.
